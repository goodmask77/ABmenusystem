# 🚀 快速部署指南

## 使用方式

修改完程式碼後，執行以下任一命令即可自動部署：

### 方式一：使用 npm 腳本（最簡單）

```bash
npm run deploy
```

或快速自動部署（使用時間戳記）：

```bash
npm run deploy:auto
```

### 方式二：使用 Node.js 腳本

```bash
node auto-deploy.js "你的 commit message"
```

### 方式三：使用 Bash 腳本

```bash
./deploy.sh "你的 commit message"
```

## ✨ 自動化流程

執行部署命令後，系統會自動：

1. ✅ 檢查變更的檔案
2. ✅ 加入所有變更 (`git add -A`)
3. ✅ 提交變更 (`git commit`)
4. ✅ 推送到 GitHub (`git push`)
5. ✅ Vercel 自動部署（如果已連接）

## 📝 範例

```bash
# 修改完程式碼後
npm run deploy

# 或自訂 commit message
npm run deploy "修復刪除功能問題"
```

## ⚙️ 設定說明

### 首次使用前

1. **設定 Git 使用者資訊**（如果還沒設定）：
   ```bash
   git config --global user.name "你的名字"
   git config --global user.email "你的email"
   ```

2. **設定 GitHub 認證**：
   - 使用 SSH key（推薦）
   - 或使用 GitHub Personal Access Token

### Vercel 設定

確保 Vercel 專案已連接 GitHub，這樣推送後會自動部署。

## 📚 詳細說明

查看 [DEPLOY.md](./DEPLOY.md) 了解更多詳細資訊。

