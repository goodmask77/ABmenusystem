# 📖 使用指南

## 🚀 完成功能修復或更新後的快速推送

### 方式一：使用 npm 腳本（最推薦）

```bash
# 完整部署流程（會詢問 commit message）
npm run deploy

# 快速自動推送（使用時間戳記）
npm run push

# 或快速保存
npm run save
```

### 方式二：使用快速推送腳本

```bash
# 使用預設 commit message
./quick-push.sh

# 或自訂 commit message
./quick-push.sh "修復刪除功能問題"
```

### 方式三：使用自動部署腳本

```bash
# Node.js 版本（推薦）
node auto-deploy.js "你的 commit message"

# 或 Bash 版本
./deploy.sh "你的 commit message"
```

## ⚡ 一鍵完成所有步驟

所有腳本都會自動執行：

1. ✅ `git add -A` - 加入所有變更
2. ✅ `git commit -m "..."` - 提交變更
3. ✅ `git push origin main` - 推送到 GitHub
4. ✅ Vercel 自動部署（如果已連接）

## 🎯 推薦工作流程

### 日常開發

```bash
# 1. 修改程式碼
# 2. 完成後執行
npm run push
```

### 功能更新

```bash
# 1. 完成功能開發
# 2. 執行部署
npm run deploy "新增 Realtime 同步功能"
```

### 快速修復

```bash
# 1. 修復問題
# 2. 快速推送
./quick-push.sh "修復刪除後恢復的問題"
```

## 🔧 進階設定

### 啟用自動推送（每次 commit 後自動 push）

```bash
./auto-push.sh enable
```

啟用後，每次執行 `git commit` 都會自動推送到 GitHub。

### 停用自動推送

```bash
./auto-push.sh disable
```

## 📝 範例

```bash
# 修復完 bug 後
npm run push

# 新增功能後
npm run deploy "新增訂單匯出功能"

# 快速更新
./quick-push.sh
```

## ⚠️ 注意事項

1. **首次使用**：確保已設定 Git 使用者資訊和 GitHub 認證
2. **認證問題**：如果 push 失敗，請檢查 SSH key 或 Personal Access Token
3. **Vercel**：確保 Vercel 已連接 GitHub 以啟用自動部署

## 🎉 就是這麼簡單！

修改完程式碼 → 執行 `npm run push` → 完成！✅

