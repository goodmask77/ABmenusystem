# ⚡ 快速推送指南

## 🎯 完成功能修復或更新後，一鍵推送到 Git

### 最簡單的方式（推薦）

```bash
npm run push
```

就這麼簡單！一個命令完成所有步驟：
- ✅ 自動加入所有變更
- ✅ 自動提交
- ✅ 自動推送到 GitHub
- ✅ 觸發 Vercel 自動部署

### 其他方式

```bash
# 方式二：快速保存
npm run save

# 方式三：完整部署（可自訂訊息）
npm run deploy "你的 commit message"

# 方式四：使用快速推送腳本
./quick-push.sh "你的 commit message"
```

## 📋 工作流程

```
1. 修改程式碼
   ↓
2. 執行 npm run push
   ↓
3. 自動完成所有步驟
   ↓
4. 完成！✅
```

## 💡 提示

- **日常使用**：`npm run push` 就夠了
- **重要更新**：使用 `npm run deploy "描述性訊息"`
- **自動推送**：執行 `./auto-push.sh enable` 後，每次 commit 都會自動 push

## 🚀 開始使用

現在就試試看：

```bash
npm run push
```

